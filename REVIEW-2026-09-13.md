# مراجعة اكتمال VisitFlow — 13 سبتمبر 2026

## الحكم

توجد إضافات كبيرة في المراحل 1–5 والمعرفة وواجهات ويندوز، لكن الخطة ليست مكتملة، ولا تزال هناك مشاكل في حفظ البيانات وصلاحيات الفريق. هذه مراجعة مركزة للكود والبناء، وليست اختبار قبول كاملًا لكل واجهة أو تكامل.

## التحقق الذي أُجري الآن

- نجح `:app:testDebugUnitTest` للاختبارات الـ98 الموجودة.
- نجح `:app:assembleRelease`.
- نجح `:desktop:compileKotlin`.
- اختبار مراجعة إضافي أعاد إنتاج فقدان بيانات عند تكرر تعارض السجل وفشل كما هو متوقع من العيب.
- لم أُعد تشغيل اختبارات Firestore في هذه المراجعة، ولم أتحقق من القواعد المنشورة أو جهازين أو اتصال AI فعلي.
- لم أغير كود الإنتاج. أضفت ملفي اختبار تحت tmp وهذا التقرير فقط.

## مشاكل يجب إصلاحها قبل اعتبار النسخة جاهزة

### 1. فقدان تعديل عند تكرر التعارض — مثبت باختبار

الموقع: `app/src/main/java/com/sales/visits/SyncMerge.kt:79`، والمنطق نفسه يحتاج مراجعة على ويندوز.

إذا كان `v1~c` موجودًا من تعارض سابق، وغيّر جهازان `v1` إلى قيمتين مختلفتين، شرط `if (!ids.contains(cid))` يمنع حفظ النسخة الخاسرة الجديدة. يبقى الفائز ونسخة التعارض القديمة، وتختفي إحدى القيمتين الجديدتين، بدون زيادة عدّاد التعارض لهذه الحالة.

الاختبار: `tmp/review-tests/SyncMergeReviewTest.kt`. يدخل نسختين B وC مع نسخة تعارض قديمة Z، ويشترط بقاء B وC وZ. فشل التحقق.

الإصلاح المطلوب: حفظ كل مراجعة متعارضة بهوية لا تصطدم بنسخة سابقة، مع تقارب الدمج وعدم تضاعف النسخ عند إعادة المحاولة. اختبر تعارضًا ثانيًا وثالثًا، والدمج المتكرر على الجهازين.

تشغيل الاختبار بإعداد JDK 17 وGradle المحلي:

```powershell
.\gradlew.bat :app:testDebugUnitTest --tests com.sales.visits.SyncMergeReviewTest --init-script tmp/review-tests.init.gradle --offline
```

### 2. تبديل الحساب يفقد العمل غير المرفوع — مثبت من مسار الكود

الموقع: `app/src/main/java/com/sales/visits/Store.kt:426`.

`prepareForAccount` يمسح القوائم ويحفظ القوائم الفارغة عند اختلاف الحساب السابق عن الجديد. لا توجد في هذا المسار نسخة محلية محفوظة باسم الحساب السابق. تعديل أُدخل أوفلاين أو فشل رفعه يمكن فقده عند الدخول بحساب آخر.

الإصلاح المطلوب: حفظ واسترجاع حالة محلية مستقلة لكل حساب، وعدم حذف الحالة غير المرفوعة. اختبار التبديل والعودة مع انقطاع الشبكة، ومراجعة المسار المكتبي أيضًا.

### 3. استقبال نجاح الرفع يمكن أن يستبدل تعديلًا أحدث

الموقع: `app/src/main/java/com/sales/visits/CloudAccount.kt:167`، و`desktop/src/main/kotlin/com/sales/visits/CloudSync.kt` في نجاح الرفع.

عملية الرفع تبدأ بلقطة snapshot. إذا عدّل المستخدم البيانات أثناء الشبكة، ثم أعادت المعاملة merged مختلفة عن لقطة البداية بسبب تحديث بعيد، ينفذ callback استعادة merged كاملة دون مقارنة الحالة المحلية الحالية بلقطة البداية. بهذا يمكن محو التعديل الذي حدث أثناء الرفع. كما لا يحرس callback أندرويد تبديل الحساب قبل الاستعادة.

الإصلاح المطلوب: ربط العملية بالحساب/الجلسة، ومصالحة التغييرات المحلية الأحدث قبل التطبيق بدل استبدالها. اختبار مؤجل يتحكم في ترتيب: بدء رفع → تعديل محلي/تبديل حساب → وصول نتيجة.

### 4. رفض إصدار بيانات أحدث يتحول إلى رفع النسخة المحلية

المواقع: `Store.kt:402` و`CloudAccount.kt:160`، ومسار push المقابل في ويندوز.

`threeWayMerge` يرفض إصدارًا أحدث ويعيد null، لكن الرفع يستخدم `?: snapshot` ثم يكتب اللقطة المحلية. إذًا فشل الدمج لا يمنع الكتابة، ويمكن لنسخة أقدم أن تستبدل بيانات أحدث بما تفهمه فقط.

الإصلاح المطلوب: إيقاف الرفع عند عدم توافق الإصدار أو فشل فك البيانات، وإظهار طلب تحديث/خطأ مناسب. اختبر أن snapshot بإصدار مستقبلي لا يتغير على الخادم ولا يتقدم last عند رفضه.

### 5. إزالة المندوب لا تسحب كل صلاحيات الكتابة

الموقع: `firestore.rules:78`.

فرع السماح للمُسند إليه بتغيير done يتحقق من `assignedTo` والحقول المتغيرة، لكنه لا يتحقق من العضوية الحالية. المستخدم الذي أُزيل من الشركة وما زال لديه إسناد باسمه يستطيع محاولة تعديل done مباشرة حتى إن رُفضت القراءة الجديدة.

الإصلاح المطلوب: اشتراط المصادقة والعضوية الحالية في هذا الفرع والتحقق من نوع done. إضافة اختبار: إنشاء إسناد لمندوب → إزالة عضويته → محاولة update مباشرة تُرفض. كذلك منع إزالة/تخفيض آخر مدير ما زال في الواجهة فقط وليس ضمانًا خلفيًا.

## نطاق لم يكتمل

- المشاركة الكاملة للعملاء والفرص والأنشطة على مستوى الشركة (6.2).
- رفع وتنزيل المرفقات بين الأجهزة (6.3).
- تكامل البريد والتقويم بالتفويض المناسب (6.4).
- المشروعات الكبرى وخريطة الحساب عند إدخالها في النطاق الفعلي (6.6).
- تحديث ويندوز التلقائي.
- تكافؤ واجهات ويندوز: توجد قوائم للفرص والعروض، لكن عرض القوائم لا يثبت وجود محررات وتدفقات مكافئة لأندرويد.
- اختبارات الأجهزة للمزامنة والتذكيرات والـAI والترقية؛ لا تُعتبر مكتملة بنجاح اختبارات المنطق.

## تجهيز إصدار أندرويد

`app/build.gradle.kts` و`update.json` ما زالا 1.7 / versionCode 9، نفس الأرقام التي كانت قبل إضافات Sales OS. الجهاز المثبت عليه البناء 9 لن يكتشف إصدارًا أعلى بهذه الأرقام. يلزم اختيار رقم بناء أعلى من المنشور فعلًا، وتجهيز الحزمة وملف التحديث واختبار الترقية. لم أتحقق من حالة النشر البعيد ولم أنشر شيئًا.

## الخطوة التالية

إصلاح مشاكل حفظ البيانات والصلاحيات أولًا واختبارها، ثم اختبار يوم عمل فعلي على الجهازين. حدّث وصف المرحلة 1 وحالة الجاهزية في UX-ROADMAP بناء على هذه النتائج؛ نجاح الاختبارات الحالية لا يغطي الحالات التي أظهرتها هذه المراجعة.
