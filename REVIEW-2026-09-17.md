# VisitFlow — إعادة المراجعة، 17 سبتمبر 2026

الإصلاحات السابقة حققت تقدمًا واضحًا، لكن المزامنة وعزل الحسابات والمساعد الجديد ما زالوا يحتاجون التصحيحات أدناه قبل اعتبار العمل مكتملًا.

## نطاق التحقق

- راجعت إصلاحات commit `517fa59` وإضافة المساعد بعد `ad17dbb` مع التغييرات المحلية الموجودة أثناء القراءة. آخر commit ظهر عند إنهاء المراجعة: `aaaf7ca`، مع استمرار تعديلات محلية أخرى.
- توجد تعديلات متزامنة من خارج المراجعة على ملفات المساعد ورقم الإصدار وAPK؛ لذلك نتيجة البناء تخص التشغيل الذي نفذته، ولا تثبت سلامة كل تعديل وصل بعد بدايته.
- في القراءة الأخيرة تغيّر مزود المساعد من OpenAI إلى Gemini. أعدت فحص مسار الإرسال وبطاقة الإجراءات؛ النتائج المتعلقة بهما أدناه ما زالت قائمة، ولم يدخل تبديل المزود الأخير ضمن الاختبارات التي اكتملت سابقًا.
- **101 اختبار JVM ناجح، صفر فشل**: تشمل اختبار المراجعة السابق الذي كان يكشف فقدان التعديل عند تكرر التعارض.
- **27 اختبار Firestore ناجحًا**، ومعها **3 اختبارات المراجعة السابقة ناجحة**؛ الاختبارات الثلاثة تتداخل في التغطية مع الاختبارات الدائمة، وليست 30 حالة مختلفة.
- نجح التحقق من ترجمة نسخة Windows. تشغيل Android اجتاز الترجمة والاختبارات ووصل إلى تصغير Release؛ جلسة التشغيل أصبحت غير متاحة قبل استلام النتيجة النهائية، لذلك لا أسجل بناء Release كاملًا كنجاح مؤكد لهذه المراجعة. وجود APK بالتزامن مع عمليات بناء خارجية لا يكفي لنسب نجاحه إلى هذا التشغيل.
- النتائج الجديدة أدناه مبنية على تتبع الكود؛ لم أنفذ سيناريوهات الحسابات أو الشبكة على أجهزة فعلية، ولم أستخدم مفاتيح AI أو بيانات المستخدم السحابية.

## ما اتصلح من المراجعة السابقة

| البند | النتيجة الحالية |
|---|---|
| صلاحيات العضو المحذوف | اتصلحت في القواعد المحلية وثبتت بالاختبارات. نشر القواعد لم يُتحقق منه؛ خارطة الطريق تقول إنه ما زال مطلوبًا. |
| فقدان النسخة عند التعارض الثاني | اتصلح، واختبار المراجعة القديم نجح. |
| محو البيانات غير المرفوعة عند تبديل الحساب | أُضيف حفظ مستقل للحساب الخارج واسترجاع للداخل. توقيت العزل على Android ما زال به مشكلة موضحة أدناه. |
| تطبيق نتيجة رفع قديمة | أُضيفت مصالحة للتعديلات أثناء الرفع وفحص UID. بقيت نافذة فقدان تعديل على Windows. |
| الكتابة فوق بيانات إصدار أحدث | مسار الرفع صار يرفض الدمج غير المتوافق. أول تنزيل ما زال يمكن أن يُعلن نجاحًا غير حقيقي. |
| تكرار إضافة حدث التقويم | صار يستخدم الحدث الموجود ويمنع تكرار طلب الإضافة الجاري. |
| نهاية الحدث طوال اليوم | أصبحت اليوم التالي. تحقق بالكود، دون اتصال Calendar فعلي. |
| تنزيل المرفق مباشرة للملف النهائي | صار يستخدم ملفًا مؤقتًا ثم ينقله؛ لم تُختبر مقاطعة تنزيل فعلي. |

## النتائج المتبقية، مرتبة بالأولوية

### 1. [P1] عزل حساب Android ينتظر رد السيرفر

الموقع: `app/src/main/java/com/sales/visits/CloudAccount.kt:63`، `:79`، `:110`، `:243`.

`connect` يغيّر المستخدم الحالي فورًا، لكن `prepareForAccount` لا تُستدعى إلا عند أول snapshot من السيرفر. الرد من الذاكرة المحلية يُرفض قبل أول مزامنة. عند الانتقال من A إلى B مع تأخر Firestore، يبقى محتوى A داخل Store وتستطيع شاشات B قراءته وتعديله. حفظ ملف B الشخصي في هذه الفترة يستدعي `upload(store.cloudSnapshot())` مباشرة دون شرط اكتمال عزل الحساب، فيمكن أن يرفع بيانات A إلى B.

المطلوب: حفظ/تبديل الحالة المحلية عند انتقال هوية الحساب نفسه، قبل عرضها أو السماح برفعها، وحماية مسار الرفع المشترك من حالة حساب لم يُجهز بعد. لا تعتمد على وصول الشبكة لتنفيذ العزل.

اختبار القبول: بدّل A→B مع تعطيل اتصال Firestore بعد نجاح المصادقة؛ لا تظهر سجلات A، ولا يرفع حفظ ملف B أي سجل من A، وتظل تعديلات A غير المرفوعة موجودة عند العودة إليه.

### 2. [P1] Windows يقرأ الحالة ثم ينتظر الخيط الرئيسي قبل تطبيقها

الموقع: `desktop/src/main/kotlin/com/sales/visits/CloudSync.kt:213`–`:218`.

المصالحة الجديدة تقرأ `localNow` وتنتج `reconciled` على IO، ثم تنتقل إلى Main لاستعادة النتيجة. إذا عدّل المستخدم سجلًا على Main بين القراءة والاستعادة، وكانت هناك تغييرات سحابية تستدعي الاستعادة، تطبق لقطة لا تشمل تعديله. فحص الحساب أيضًا يحدث قبل الانتقال إلى Main، فلا يغطي تغيير الحساب في الفترة نفسها.

المطلوب: اجعل فحص الجلسة وقراءة الحالة الأحدث ودمجها وتطبيقها عملية متصلة على Main، دون نقطة انتظار بينها. اختبر تعديلًا محليًا يصل بعد انتهاء الشبكة وقبل التطبيق.

### 3. [P2] أول مزامنة قد تعرض «تمت المزامنة» رغم رفض إصدار أحدث

الموقع: `app/src/main/java/com/sales/visits/CloudAccount.kt:117`–`:129`، والمسار المقابل `firstSync` في Windows.

عندما `local == last` يُختار `remote` مباشرة دون فحص الإصدار. `restoreCloudSnapshot` ترفض الإصدار المستقبلي وتعيد false، لكن المستدعي يهمل النتيجة، ويحفظ remote كآخر حالة متزامنة ويعرض synced. تظل الشاشة تعرض البيانات القديمة دون طلب التحديث.

المطلوب: افحص نجاح الاستعادة قبل تحديث last أو حالة النجاح، واعرض outdated عند رفض الإصدار. اختبار القبول: local مطابق لآخر مزامنة وremote بإصدار أكبر من المدعوم؛ لا يتغير last ولا تظهر synced.

### 4. [P2] المساعد الجديد يختار مهمة عشوائية عند غموض الطلب

الموقع: `app/src/main/java/com/sales/visits/AssistantScreen.kt:249`–`:263`.

إعادة الجدولة تستخدم `firstOrNull` على تطابق نص المهمة أو العميل. لو العميل لديه متابعتان، تختار الأولى دون أن تطلب تحديد المقصودة. والأسوأ: لو أعاد النموذج query/text/customer فارغة، فإن `contains("")` يطابق كل المهام؛ تُختار أول مهمة مفتوحة. بطاقة التأكيد تعرض عنوان المهمة وتاريخها، ولا تعرض اسم العميل إذا كان العنوان موجودًا، فلا تميز متابعتين بالعنوان نفسه.

المطلوب: رفض البحث الفارغ، وقبول تطابق وحيد فقط أو عرض اختيار صريح عند التعدد، وإظهار العميل والتاريخ والعنوان قبل التأكيد.

### 5. [P2] لا يوجد فحص لصحة تاريخ/وقت الإجراء القادم من AI

الموقع: `app/src/main/java/com/sales/visits/AssistantScreen.kt:240`–`:243` و`:262`–`:263`، مع `Store.kt:599` و`:639`.

التأكيد يتحقق من وجود النص أو التاريخ فقط؛ `addTask` و`rescheduleTask` تحفظان التاريخ كما هو. نتيجة نموذج مثل `2026-02-30` أو `tomorrow` أو وقت `25:90` يمكن تأكيدها وحفظها، فتخرج المهمة من العرض المعتاد لليوم أو تفشل إضافتها للتقويم. طلب تنسيق محدد في الـprompt لا يقوم مقام التحقق.

المطلوب: استخدام LocalDate/LocalTime للتحقق قبل الحفظ في مسار الكتابة المناسب، وطلب تصحيح القيمة بدل إظهار نجاح. اختبر قيمة صحيحة وتاريخًا مستحيلًا ووقتًا خارج المدى.

### 6. [P2] المساعد الجديد لا يطلب الإقرار بإرسال بيانات العملاء

الموقع: `app/src/main/java/com/sales/visits/AssistantScreen.kt:78`–`:87`، و`VoiceAssistantScreen.kt:73` وما بعدها.

مسار المساعد الجديد يفحص مفتاح المزود فقط ثم يرسل السياق، دون بوابة الإقرار المستخدمة في بقية ميزات AI، حتى عندما `aiConsent=false`. في القراءة الأخيرة أصبح يستخدم Gemini. السياق الحالي يشمل معلومات اليوم، وقد يشمل أرقام العملاء وجهات الاتصال وملاحظاتهم والزيارات والفرص. وجود المفتاح لا يفعّل الإقرار المخزن ولا يغني عن توضيح نطاق البيانات المرسلة.

المطلوب: إقرار واضح قبل أول إرسال يذكر المزود والبيانات المرسلة، وإعادة استخدام آلية الموافقة الموجودة بعد تكييف النص والنطاق. لا ترسل السياق إذا ألغى المستخدم.

## أعمال لم تصبح مكتملة بمجرد نجاح الاختبارات

- تأكيد نشر قواعد Firestore المصححة؛ الاختبار هنا محلي فقط.
- التقويم ما زال اتجاهًا واحدًا، وفشل التحديث/الحذف لا يُحفظ لإعادة المحاولة. جرى تصحيح الوصف ولم تُنفذ المزامنة في الاتجاه الآخر.
- تجربة حسابين وجهازين مع انقطاع الشبكة، وتفويض Drive/Calendar الفعلي.
- لا يوجد ضمن تشغيل الاختبارات الحالي تحقق فعلي من المساعد الجديد مع المزود أو واجهة الصوت.

لم أغيّر كود الإنتاج أو أنشر قواعد أو حزم؛ أضفت تقرير المراجعة وشغّلت أدوات التحقق فقط.
