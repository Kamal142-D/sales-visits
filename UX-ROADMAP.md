# VisitFlow — خطة الاستكمال والحالة

الحالة موضّحة أمام كل بند: ✅ منفّذ · 🧪 مختبَر (يدويًا) · 🚀 منشور · ⬜ متبقٍ.
البناء يتم بـ`JAVA_HOME = JDK 17` ثم `./gradlew.bat assembleRelease`.

## المرحلة 1 — تثبيت النسخة وتجهيز التحديث

### 1.1 إصلاح حفظ الزيارة ✅ (يحتاج اختبار جهاز 🧪)
- الزيارة الفارغة بالكامل تُغلق بدون حفظ أو اهتزاز. ✅
- أي مدخلات (ملاحظات عربي/إنجليزي، صور، قائمة مهام، نتيجة، خطوة تالية) لا تضيع عند الرجوع؛ يظهر حوار «تخرج من غير حفظ؟». ✅
- اسم العميل ناقص → تلوين الحقل ورسالة، مع الاحتفاظ بكل المدخلات. ✅
- نتيجة الزيارة أو الخطوة التالية ناقصة → منع الحفظ واهتزاز الزر الدائري. ✅
- نفس القواعد على زر الرجوع/الحفظ وعلى تعديل زيارة موجودة. ✅

### 1.2 مراجعة التعديلات الموجودة ✅
- العملاء تحت Edit Profile (نفس البطاقة). ✅
- Cloud Sync في الإعدادات فقط. ✅
- Quick Result وNext Step في زر دائري مقابل زر التسجيل، والموضع ينعكس بالعربية. ✅
- About يعرض رقم الإصدار الفعلي تلقائيًا. ✅

### 1.3 تجهيز Release ✅ (النشر على GitHub ⬜ — يحتاج إذن الدفع/Push)
- الإصدار الآن `versionName 1.7` / `versionCode 9`. ✅
- `update.json` حُدِّث إلى 9/1.7 مع ملاحظات؛ كان 6/1.5. ✅
- تنبيه Google API key: هو مفتاح عميل Firebase داخل `app/google-services.json` وهو غير سري ويُشحن مع التطبيق؛ الإجراء الموصى به تقييد المفتاح (اسم الحزمة + SHA‑1) في Google Cloud ثم إغلاق التنبيه. (لم تُعرض القيم.) ⬜ إجراء بشري
- النشر: بعد الدفع إلى `main` يجب التأكد أن التطبيق يكتشف التحديث (json.versionCode > المثبّت) وينزّل `app-release.apk`. ⬜

## المرحلة 2 — تجربة المندوب والتذكيرات والتقويم

### المتبقي من تجربة المندوب
1. هدف يومي قابل للتحديد (منفصل عن عدد عناصر الخطة) مع تقدّم واضح. ✅ — بطاقة «الهدف اليومي» في شاشة اليوم، تقيس الزيارات الفعلية المسجّلة/الهدف بحلقة تقدّم، وقابلة للتعديل.
2. قوالب ملاحظات سريعة قابلة للاستخدام والتعديل. ✅ — شرائح قوالب تحت حاويتي الملاحظة، والقالب يدخل الحاوية المطابقة للغته؛ شيت لإضافة/حذف القوالب.
3. تحسين سجل العميل عند الحاجة. ⬜ (السجل الحالي كافٍ حاليًا)

### التذكيرات ✅ (اختبار جهاز/إعادة تشغيل 🧪)
- اختيار وقت التذكير من الإعدادات بدل 9 صباحًا الثابتة. ✅
- نصوص التنبيه تتبع لغة التطبيق. ✅
- إعادة الجدولة عند تعديل الموعد/الإلغاء (موجودة في upsert/cancel). ✅
- توضيح حالة التنبيهات (مفعّلة/تحتاج إذن) — موجود كزر جرس + بطاقة تفعيل. ✅
- اختبار التنبيه والتطبيق مغلق وبعد إعادة التشغيل. 🧪 يحتاج جهازًا.

### التقويم ✅
- عرض شهري بعلامات للأيام التي فيها زيارات (نقطة خضراء) أو متابعات (نقطة داكنة) أو متابعة متأخرة (نقطة حمراء). ✅
- الضغط على اليوم يفتح خطة ذلك اليوم (يضبط اليوم المختار). ✅
- تمييز اليوم الحالي والمحدّد. ✅
- الحفاظ على شريط الأيام السريع (زر تبديل للتقويم). ✅

## المرحلة 3 — الحساب والمزامنة والبروفايل (جزئيًا ✅)
- صورة بروفايل فعلية بدل الحروف. ✅ — تُختار من معرض الصور في Edit Profile، تُحفظ محليًا على الجهاز لكل حساب، وتظهر في البروفايل. (مزامنتها بين الأجهزة تتبع بند صور الزيارات أدناه.)
- عزل البيانات عند تبديل الحساب. ✅ — `prepareForAccount(uid)`: عند أول مزامنة لحساب مختلف عن آخر حساب على الجهاز، تُمسح بيانات الحساب السابق قبل الدمج (زيارات/عملاء/خطة/مهام/مخزون/صورة). أول تسجيل (ضيف→حساب) يحتفظ بالبيانات ويرفعها.
- إدخال المخزون والمهام في النسخ الاحتياطي والمزامنة. ✅ — أُضيفا إلى `AppBackup` و`mergeBackups`، مع الحفاظ على توافق النسخ القديمة (حقول بقيَم افتراضية + `ignoreUnknownKeys`).
- قواعد Firebase الأمنية. ✅ (ملف `firestore.rules` جاهز — نشر بشري ⬜) — كل حساب يقرأ/يكتب `users/{uid}` فقط.
- اختبار أوفلاين ثم مزامنة، وتعارض تعديلات جهازين، والحذف. 🧪 يحتاج جهازين — منطق الدمج موجود (آخر تعديل للسجل نفسه يفوز محليًا).
- استعادة الحساب على جهاز جديد. 🧪 يحتاج جهازًا.
- مزامنة صور الزيارات الفعلية (رفع/تنزيل عبر Firebase Storage). ⬜ — المسارات المحلية لا تنقل الصور بين الأجهزة؛ يحتاج تخزينًا سحابيًا (بند مستقل أكبر).
- قوالب الملاحظات تبقى محلية (تفضيل مستخدم، غير مُدرجة في المزامنة عمدًا). ✅

## المرحلة 4 — الشركات وإدارة الفريق (طبقة العضوية ✅ · التوزيع ⬜)

### 4.1 الشركة والفريق والصلاحيات (الأساس ✅ — يحتاج نشر القواعد + اختبار أجهزة)
- **نموذج البيانات + التنفيذ في الخلفية**: `firestore.rules` يفرض العزل فعليًا (مش إخفاء أزرار): كل حساب يقرأ/يكتب `users/{uid}` فقط؛ بيانات الشركة يقرأها أعضاؤها فقط؛ تغيير الأدوار/تعديل الشركة للمدير فقط. ✅
- **CompanyRepository** (Firestore): إنشاء شركة (يصبح المُنشئ مديرًا)، كود دعوة عشوائي، الانضمام بكود (مندوب)، متابعة العضوية والأعضاء لحظيًا، تغيير دور عضو (مدير فقط)، مغادرة الشركة، ومصالحة الإزالة. ✅
- **شاشة الفريق** في البروفايل: إنشاء/انضمام، اسم الشركة والدور، كود الدعوة (نسخ)، قائمة الأعضاء بأدوارهم، وأزرار المدير لترقية/تخفيض عضو. ✅
- **الحفاظ على الحسابات الفردية والبيانات الحالية**: الميزة إضافية بالكامل ولا تلمس مزامنة الحساب الفردية. ✅
- **متبقٍ**: نشر `firestore.rules` على Firebase (بشري ⬜)، واختبار حسابين فعليين (مدير + مندوب) للتأكد من الإنشاء/الانضمام/الأدوار والعزل. 🧪

### 4.2 توزيع العمل (طبقة الإسناد ✅ — يحتاج نشر القواعد + اختبار أجهزة)
- **مهام مُسندة مشتركة على مستوى الشركة** `companies/{cid}/assignments` (إضافية، لا تلمس مزامنة الحساب الفردية): المدير يُسند عميلًا (اسم/هاتف/عنوان/ملاحظة) لمندوب، يعيد الإسناد، أو يحذف؛ المندوب يشوف مهامه ويعلّمها «تمّت» ويتصل/يفتح الموقع. ✅
- **CompanyRepository**: `addAssignment/setAssignmentDone/reassign/deleteAssignment/myAssignments` + متابعة لحظية. ✅
- **قواعد الأمان**: المدير ينشئ/يعيد/يحذف؛ المندوب المُسنَد له يحدّث عنصره فقط؛ القراءة للأعضاء. ✅ (نشر بشري ⬜)
- **شاشة الفريق**: المدير يشوف كل اللوحة + زر «إسناد مهمة»؛ المندوب يشوف مهامه فقط. ✅
- **متبقٍ**: نقل **كامل** بيانات العملاء/الزيارات لمجموعات الشركة (بدل الإسناد المستقل) لو احتجنا مشاركة السجل الكامل، واختبار حسابين فعليين. 🧪⬜

### 4.3 الأهداف والتقارير (لوحة المدير) ✅ فوق طبقة الإسناد — يحتاج نشر القواعد + اختبار
- **لوحة المدير** (للمدير فقط): ملخص المهام (مُسندة/مكتملة/متبقّية)، **هدف الفريق** قابل للتحديد بشريط تقدّم، و**تفصيل حسب المندوب** (شريط إنجاز done/assigned لكل مندوب). ✅
- **فلاتر** على اللوحة: حسب المندوب وحسب الحالة (الكل/المتبقّي/المكتملة). ✅
- **الصلاحيات**: هدف الفريق يعدّله المدير فقط (مفروض في القواعد `update if isManager`)؛ المندوب يشوف مهامه فقط. ✅
- **ملاحظة**: التقارير هنا فوق **لوحة الإسناد المشتركة** (وهي البيانات المشتركة فعلاً بين الحسابات). تقارير نتائج الزيارات الفعلية عبر المندوبين تحتاج مشاركة السجل الكامل (إعادة هيكلة 4.2 الكاملة). ⬜
- **متبقٍ**: نشر القواعد + اختبار حسابين. 🧪

## المرحلة 5 — فرص البيع ✅ (سجل مستقل — Kanban اختياري لاحقًا)
- **سجل فرصة مستقل** (مش نتيجة زيارة): عنوان، ربط بالعميل (عبر منتقي العملاء)، القيمة والعملة، المسؤول، الخطوة التالية وتاريخها، ملاحظات. ✅
- **حالات بسيطة**: جديدة / متابعة / تم البيع / لم تتم. ✅
- **صفحة الفرص** في البروفايل: ملخّص (عدد الفرص، القيمة المفتوحة، المكتسبة) + **عرض خط أنابيب مجمّع حسب الحالة**، وزر + للإضافة، وتعديل/حذف عبر شيت. ✅
- **مضمّنة في النسخ الاحتياطي والمزامنة** (AppBackup + merge + عزل تبديل الحساب)، مع توافق النسخ القديمة. ✅
- **Kanban**: مؤجّل عمدًا حتى يثبت الاستخدام (الخطة تقول يُضاف لو لزم). ⬜

## المرحلة 6 — نسخة Windows (exe + مزامنة ✅ — يحتاج نشر القواعد + اختبار)
- **exe**: ضبط الحزم لإنتاج مثبّت `VisitFlow-1.7.0.exe` (+ MSI) ونسخة محمولة (app-image). ✅
- **ربط الحساب والمزامنة**: نظراً لأن Firebase SDK أندرويد فقط، النسخة المكتبية تستخدم **Firebase REST** (Auth + Firestore) وتزامن **نفس الوثيقة** `users/{uid}/private/state` (حقل snapshot) اللي بيستخدمها الأندرويد — فنفس الحساب يشتغل على الكمبيوتر والموبايل. ✅
  - تسجيل دخول/إنشاء حساب/خروج من الإعدادات، تجديد التوكن تلقائيًا، سحب عند الدخول + **Poll كل 30 ثانية** (REST مفيهاش realtime)، ورفع عند التعديل (debounce).
  - **بدون فقدان بيانات**: وحّدت موديلات الديسكتوب مع الأندرويد وأضفت تمرير المهام/المخزون/الفرص round-trip، + عزل تبديل الحساب.
  - الأمان: بيمس `users/{uid}` بس (نفس قواعد `firestore.rules`).
- **متبقٍ**: رقم إصدار/تحديث تلقائي للنسخة المكتبية، واختبار فعلي لنفس الحساب على الجهازين بعد نشر القواعد. 🧪⬜

## الهوية البصرية
- الأيقونة الحالية مسطّحة (علامة اقتباس + ٣ سطور، Adaptive + monochrome). ✅
- طلب أيقونة 3D قابلة للتعديل في Figma يبقى بندًا مستقلًا يحتاج اعتماد قبل الاستبدال. ⬜

---

## Sales OS — المرحلة 1 (تثبيت البيانات) — وحدة 1: روابط ثابتة + ترحيل آمن ✅ (منطق مختبَر)

مرجع الخطة: `VISITFLOW-SALES-OS-PLAN.md` (المرحلة 1، وحدات 1.2 + 1.3 + جزء 1.6).

- **روابط ثابتة (`customerId`)** بدل الاسم فقط: أُضيف حقل `customerId` للزيارة والطلب والفرصة وعنصر الخطة/المهام، و`id` ثابت لكل جهة اتصال. الاسم يبقى **لقطة عرض**، والربط الحقيقي بالـ id — فتغيير اسم العميل ما بيفصلش سجلاته. ✅
- **ربط غير ملتبس فقط**: الترحيل يربط السجل بالعميل **لو الاسم يطابق عميلًا واحدًا بالضبط**؛ الاسم الملتبس (عميلان بنفس الاسم) يفضل **غير مربوط** بدل التخمين. ✅ (شاشة «يحتاج ربطًا» بند لاحق في المرحلة 2)
- **ترحيل متكرر بأمان (idempotent)**: بيشتغل كل تشغيل، وتشغيله تاني = لا تغيير ولا IDs جديدة. مُغلّف في دالة نقية `DataMigration` بدون أي اعتماد على Android. ✅
- **إصدار البيانات**: رُفع لـ `CURRENT_BACKUP_VERSION = 2`. القارئ يقبل `1..2` ويرحّل للأعلى، ويرفض إصدارًا مستقبليًا لا يفهمه (يمنع الكتابة فوقه). ✅
- **توحيد أنواع الزيارة (1.6 جزئي)**: `mapLegacyVisitType` بيحوّل صريحًا قيم الديسكتوب القديمة (FIRST/FOLLOW/DELIVER/COLLECT/COMPLAINT/OTHER) للأنواع الـ12 — **«FOLLOW» بقت متابعة عميل، مش «زيارة أولى»**. ✅
- **إجمالي المشتريات لكل عملة**: `purchasedByCurrency` — مفيش جمع بين ريال ودولار في رقم واحد؛ صفحة العميل بتعرض سطرًا لكل عملة. ✅
- **توافق Windows (round-trip)**: أُضيفت نفس الحقول لموديلات الديسكتوب فمابتتشالش وقت المزامنة، وقبول الإصدار `1..2`، وإصلاح تصدير الديسكتوب اللي كان بيسقط المهام/المخزون/الفرص/الطلبات. ✅

**ما اختُبر**: 11 اختبار JVM لوحدة `DataMigration` (تعيين النوع القديم، الربط غير الملتبس، تجاهل المطابقة الملتبسة، عدم لمس المربوط، backfill لجهات الاتصال، idempotency، ختم الإصدار) — **كلها ناجحة**. بناء الأندرويد (release) وكومبايل الديسكتوب ناجحان.
**ما لم يُختبر**: مزامنة فعلية على جهازين، وround-trip فعلي أندرويد↔ويندوز على بيانات حقيقية (يحتاج جهازين/حسابًا) — **غير مختبر**.
**ما نُشر**: لا شيء (لا Git push ولا نشر قواعد).
**متبقٍ في المرحلة 1**: 1.4 سياسة تعارض/حذف المزامنة (revision + tombstones بدل «المحلي يفوز»)، 1.5 فرض صلاحيات الشركة في الـbackend، وتوحيد enum الديسكتوب بالكامل في واجهته. 🧪⬜

## Sales OS — المرحلة 1 — وحدة 2 (1.4): سياسة تعارض/حذف المزامنة ✅ (منطق مختبَر)

بدّلت سياسة «دمج القوائم والمحلي يفوز» بـ **دمج ثلاثي (three-way merge)** بيستخدم آخر نسخة متزامنة (`last`) كأساس — بدون الاعتماد على ساعة الجهاز:

- **سجل غيّره طرف واحد** → يؤخذ الطرف المتغيّر. **الاثنين ما غيّروش** → يفضل. ✅
- **حذف من طرف والآخر ما غيّرش** → يُحترم الحذف (الأساس بيميّز «محذوف» عن «لسه ما اتشافش»، فمفيش إحياء صامت للمحذوف). ✅
- **حذف من طرف مقابل تعديل من الطرف الآخر** → **يتحفظ التعديل** (مفيش فقدان للبيانات لصالح حذف). ✅
- **الطرفان عدّلا نفس السجل باختلاف** → **يتحفظ الاثنين**: فائز حتمي (deterministic) يحتفظ بالـ id، والخاسر يتحفظ كنسخة تعارض `<id>~c` — فمفيش فقدان صامت، والاختيار للمستخدم (يحذف اللي مش عايزه). الاختيار **متماثل ومتقارب** فالجهازين بيوصلوا لنفس النتيجة بدون تكاثر نسخ. ✅
- **منع الكتابة فوق تعديل محلي غير مرفوع**: الاستماع للتغيّر البعيد ما بيعملش استبدال كامل تاني؛ بيعمل دمج ثلاثي. الرفع بقى **داخل Transaction** (أندرويد) و**بشرط `updateDocument.updateTime`** (ويندوز/REST) — فرفع متزامن من جهاز تاني بيتدمج مش بيتكتب فوقه، ومع فشل الشرط بيعيد القراءة والدمج والمحاولة. ✅
- **حالة المزامنة حسب النتيجة الحقيقية**: `local · syncing · synced · conflict · offline`؛ الرفع المرفوض/الكبير بيفضل `offline` مش «synced» كذّابة، والأساس `last` ما بيتقدّمش إلا مع كتابة مؤكدة. ✅
- **حد الوثيقة (~1MiB)**: الرفع الفاشل بيظهر offline؛ التقسيم لمجموعات يتأجّل للحاجة الفعلية (معلّق في الكود). ⬜

**ما اختُبر**: 8 اختبارات JVM لمحرك `SyncMerge` (سجلات مختلفة، تعارض نفس السجل + تماثل + تقارب، احترام الحذف، حذف مقابل تعديل، سجل جديد محلي، أساس فارغ) — **كلها ناجحة** (إجمالي المرحلة 1: **19 اختبار**). بناء الأندرويد وكومبايل الديسكتوب ناجحان.
**ما لم يُختبر**: سلوك فعلي على جهازين حقيقيين + توقيت الـTransaction/الـprecondition تحت تزامن حقيقي — **غير مختبر** (يحتاج جهازين/حساب + نشر القواعد).
**ما نُشر**: لا شيء.
**متبقٍ في المرحلة 1**: 1.5 فرض صلاحيات الشركة في الـbackend + اختبار بالمحاكي؛ وحفظ تعديلات الحساب السابق غير المرفوعة عند التبديل (العزل شغّال، لكن الحفاظ على غير-المرفوع عبر التبديل لسه). 🧪⬜

## Sales OS — المرحلة 1 — وحدة 3 (1.5): فرض صلاحيات الشركة في الـ backend ✅ (مختبَر بالـ Emulator)

قوّيت [firestore.rules](firestore.rules) بحيث الفرض يحصل على **سيرفر Firestore** (مش إخفاء أزرار)، وسددت الثغرات:

- **ترقية النفس ممنوعة**: صف عضوية المدير مربوط بـ `ownerUid` بتاع الشركة ومتحقّق منه **ذريًا في نفس الـbatch** عبر `getAfter` — فمحدش يقدر يعمل لنفسه صف `role: MANAGER` في شركة مش بتاعته، ولا يرقّي نفسه بـupdate. ✅
- **الانضمام بإثبات دعوة**: العضو الجديد يقدر يعمل صف `REP` **بس** لو قدّم **كود الدعوة الحالي** للشركة (الـrule بيقارنه بـ`company.inviteCode`) — فتدوير الكود بيبطّل الدعوات القديمة. ✅
- **أكواد الدعوة للمدير/المالك بس**: إنشاء/تعديل/حذف كود دعوة مقصور على مالك الشركة (وقت التأسيس) أو مدير — مش أي مستخدم مسجّل. ✅
- **المندوب محدود**: يقدر يغيّر **بس** `done` على الإسناد **بتاعه** (`hasOnly(['done'])` + `assignedTo` ثابت) — ميقدرش يعيد الإسناد، يغيّر العميل، أو يلمس شغل مندوب تاني؛ وإنشاء/حذف الإسنادات للمدير بس. ✅
- **عزل الحساب**: كل مستخدم يقرأ/يكتب `users/{uid}` بتاعه بس؛ والمطرود من الشركة بيفقد القراءة/الكتابة فورًا (الـrules بتفحص العضوية الحيّة). ✅
- **المالك ثابت**: المدير ميقدرش ينقل ملكية الشركة (`ownerUid` immutable في التعديل). ✅
- **آخر مدير**: منع تنزيل/خروج آخر مدير مطبّق **في العميل** (client guard) + رسالة `last_manager` بالعربي والإنجليزي؛ الفرض الكامل في الـbackend لهذه النقطة يحتاج Cloud Function (القواعد ماتقدرش تعدّ) — **موثّق كفجوة، مش مُدّعى**. ⬜

**ما اختُبر**: **15 اختبار بالـ Firebase Emulator** (`firestore-tests/`) — كلها ✅ ضد محرك Firestore حقيقي (v1.22.0): منع ترقية النفس، رفض/قبول الانضمام بالكود، حدود المندوب على الإسناد، تأسيس شركة بـbatch، عزل المستخدمين، أكواد الدعوة. + 19 اختبار JVM من الوحدات السابقة. بناء الأندرويد ناجح.
**ما لم يُختبر**: سلوك حقيقي على أجهزة/حسابات متعددة بعد النشر.
**ما نُشر**: **لا شيء** — القواعد **متنشرتش** على مشروعك بعد؛ لازم تعملها إنت.
**للنشر (إنت)**: من جذر المشروع: `firebase deploy --only firestore:rules` (وفعّل Email/Password في Firebase Auth لو مش مفعّل). لحد ما تنشر، **القواعد القديمة الضعيفة لسه شغّالة على السيرفر**.

## Sales OS — المرحلة 2 — وحدة 1 (2.1 + 2.2): بنك العملاء المحتملين + أدوار القرار ✅ (منطق مختبَر)

- **حالة العلاقة** (بنك prospects): كل عميل بقى له `relationship` = محتمل/عميل/مؤرشف. العميل الجديد اليدوي بيبدأ **محتمل**، والموجود يفضل **عميل**. مفيش سجل Prospect منفصل — فلتر جوّه صفحة العملاء (الكل/محتمل/عميل/مؤرشف). ✅
- **حقول تطوير العميل**: المدينة، المنطقة، الموقع الإلكتروني، مصدر التعارف، المعدات الحالية، التطبيقات/الأعمال — كلها اختيارية وتتضاف تدريجيًا. ✅
- **بحث أوسع**: بالاسم/جهة الاتصال/الهاتف + **المدينة والمجال**. ✅
- **جهات الاتصال بأدوار قرار**: كل جهة بقى لها **مسمى وظيفي** و**أدوار متعددة** (صاحب قرار/مؤثّر/فني/مشتريات/بوابة وصول/غير معروف) — اختيار متعدد بالشيبس. الأدوار مدخلة يدويًا مش مستنتجة. ✅
- **صفحة العميل (قراءة)**: بادچات (نوع العلاقة + المجال + المدينة) + كارت تفاصيل (الموقع/المصدر/المعدات/التطبيقات) لما تكون متملّية. ✅
- **توافق**: كل الحقول إضافية بـdefaults (النسخ القديمة تتقري عادي) واتضافت لموديلات الديسكتوب للـround-trip؛ الترحيل بيحافظ عليها (idempotent). ✅

**ما اختُبر**: 3 اختبارات JVM جديدة (تعيين حالة العلاقة + بقاء الحقول الجديدة عبر الترحيل + round-trip كامل) — ناجحة. **إجمالي المشروع دلوقتي: 22 اختبار JVM + 15 اختبار Emulator.** بناء الأندرويد وكومبايل الديسكتوب ناجحان.
**متبقٍ في المرحلة 2**: وحدة 2 = حقول الفرصة + مراحل البيع + سجل الانتقالات + شاشات الفرصة (2.3/2.4/2.5)، وربط جهات القرار بالفرصة، وعروض «بيانات ناقصة/بدون خطوة قادمة».

## Sales OS — المرحلة 2 — وحدة 2 (2.3 + 2.4 + 2.5): تأهيل الفرصة + مراحل البيع + سجل الانتقالات ✅ (منطق مختبَر)

- **مراحل بيع حقيقية** بدل NEW/FOLLOW: جديدة → تأهيل → فهم الاحتياج → عرض الحل/تجربة → عرض سعر → تفاوض/اعتماد → تم البيع/لم تتم + **مؤجلة** (خارج النشط). مش إجباري تمر بكلها. ✅
- **سجل الانتقالات**: أي تغيير مرحلة بيتسجّل (من→إلى + التاريخ + المنفّذ) في `history`، وبيتعرض في صفحة الفرصة. `withStage` نقية وidempotent (تغيير لنفس المرحلة = لا شيء). ✅
- **ترحيل صريح للقديم**: `mapLegacyStage` — **FOLLOW → تأهيل** (مش «جديدة» وهمية)، وWON/LOST/NEW زي ما هي، بدون اختراع تاريخ مراحل. ✅
- **حقول التأهيل** (2.3): المنتج/الحل، الاحتياج، المشكلة وتأثيرها، حالة الميزانية (+ القيمة)، توقيت الشراء المتوقع، المنافس، العائق، وربط **أصحاب القرار** من جهات اتصال العميل بالصفقة نفسها. ✅
- **نُدج بدون إجبار**: قبل مرحلة عرض السعر بيظهر اللي لسه ناقص (احتياج/منتج/صاحب قرار/توقيت/ميزانية) كتنبيه مش مانع؛ والفرصة النشطة بدون خطوة قادمة بتاريخ بتتنبّه. ✅
- **الخسارة**: سبب + تمييز «صرّح به العميل / استنتاج مني». ✅
- **شاشة الفرص**: فلتر (الكل/نشطة/تم/لم يتم/مؤجلة) + تجميع حسب المرحلة + قيمة مفتوحة = الفرص النشطة بس. ✅
- **توافق**: كل الحقول + `StageChange` إضافية بـdefaults واتضافت لموديلات الديسكتوب للـround-trip. ✅

**ما اختُبر**: 7 اختبارات JVM جديدة (تعيين المرحلة القديمة، isActive، تسجيل/تراكم/no-op للانتقالات، missingForQuote، الميزانية) — ناجحة. **إجمالي المشروع: 29 اختبار JVM + 15 اختبار Emulator.** بناء الأندرويد وكومبايل الديسكتوب ناجحان. القواعد الأمنية **منشورة** (تم النشر من الـconsole).
**متبقٍ (اختياري)**: صفحة فرصة كاملة الشاشة (بدل الشيت) لو لزم، وواجهة الديسكتوب للمراحل الجديدة (Phase 6.5)، وعروض «بيانات ناقصة» على مستوى القائمة.

## Sales OS — المرحلة 3 — وحدة 1 (3.1): سجل الأنشطة + السجل الزمني الموحّد ✅ (منطق مختبَر)

- **سجل نشاط خفيف**: تسجيل مكالمة/اجتماع/بريد/رسالة/ملاحظة لكل عميل، بجانب الزيارة. الزيارة تفضل سجلها الأصلي — الأنشطة **مبتكررهاش**. ✅
- **سجل زمني موحّد** في صفحة العميل: الزيارات + الأنشطة في قائمة واحدة، الأحدث أولًا (`mergeTimeline` نقية). ✅
- **نتائج صادقة**: المكالمة مش بتتحسب «تم الرد» إلا لو اخترتها؛ الفارغ = «—». (تم التواصل/لا رد/إعادة اتصال/تمّ). ✅
- **ربط**: النشاط بيتربط بالعميل (id ثابت) و**اختياريًا بفرصة** من فرص نفس العميل. ✅
- **توافق ومزامنة**: `Activity` أُضيفت للـAppBackup + mergeBackups + الدمج الثلاثي (SyncMerge) + الترحيل (ربط بالاسم غير الملتبس) + عزل تبديل الحساب — على **الأندرويد والديسكتوب** (round-trip). ✅

**ما اختُبر**: 5 اختبارات JVM جديدة (ترتيب السجل الزمني، round-trip، صدق النتائج، ربط الترحيل، دمج المزامنة من الجهتين) — ناجحة. **إجمالي المشروع: 34 اختبار JVM + 15 اختبار Emulator.** بناء الأندرويد وكومبايل الديسكتوب ناجحان.
**متبقٍ في المرحلة 3**: 3.2 عروض الأسعار (Quote + بنود + حالات + نسخ)، 3.3 توحيد المتابعات (ربط المهمة بالفرصة/المصدر/نتيجة الإتمام)، 3.4 الأتمتة المحددة، 3.5 شاشة اليوم الموحّدة.

## Sales OS — المرحلة 3 — وحدة 2 (3.2): عروض الأسعار ✅ (حساب مختبَر)

- **عرض سعر ببنود**: كل بند (وصف/منتج، كمية، سعر وحدة، خصم %) + ضريبة % على مستوى العرض (المستخدم يدخلها؛ **مفيش نسبة ضريبة مفترضة**). عملة واحدة للعرض. ✅
- **الحساب بالكود مش بالـAI**: `QuoteMath` — صافي البند (بعد الخصم) ثم الإجمالي الفرعي ثم الضريبة ثم الإجمالي، بسياسة تقريب معلنة (HALF_UP لخانتين) وتقريب كل بند قبل الجمع لتجنّب أخطاء Double. ✅
- **الحالات**: مسودة/أُرسل/مقبول/مرفوض/منتهي/ملغى. «تمت المشاهدة» مؤجلة. ✅
- **نُسخ (revisions)**: تعديل **بنود** عرض مُرسل بيحفظ **نسخة جديدة** (version+1، مسودة) والأصلي بيفضل زي ما استلمه العميل؛ تغيير الحالة/الملاحظات بس بيعدّل في مكانه. ✅
- **من المخزون**: اختيار منتج بياخد **سعره الحالي كلقطة** — تحديث المخزون لاحقًا مابيغيّرش العرض القديم. ✅
- **إنشاء طلب من عرض مقبول**: إجراء **صريح ومرة واحدة** (بيربط `orderId` فمفيش ازدواج)؛ القبول ≠ تحصيل ولا بينشئ طلب تلقائيًا. ✅
- **صفحة العميل**: قسم عروض الأسعار (قائمة + إجمالي كل عرض + حالته) وزر عرض جديد يفتح محرّر كامل الشاشة.
- **توافق ومزامنة**: `Quote`/`QuoteLine` أُضيفت لكل طبقات البيانات (backup + دمج ثلاثي + ترحيل + عزل الحساب) على الأندرويد والديسكتوب. ✅
- **مؤجّل** (كما تسمح الخطة): إرفاق ملف PDF للعرض (يحتاج تخزين مرفقات — Phase 6.3)، وهامش الربح (يحتاج تكلفة).

**ما اختُبر**: 9 اختبارات JVM جديدة (صافي البند/الخصم/التقريب HALF_UP، الإجمالي الفرعي/الضريبة/الإجمالي، صفر ضريبة افتراضيًا، النسخة revision، round-trip، الدمج/الترحيل). **إجمالي المشروع: 43 اختبار JVM + 15 اختبار Emulator.** بناء الأندرويد وكومبايل الديسكتوب ناجحان.
**متبقٍ في المرحلة 3**: 3.3 توحيد المتابعات (ربط المهمة بالفرصة/المصدر/نتيجة الإتمام)، 3.4 الأتمتة المحددة، 3.5 شاشة اليوم الموحّدة.

## Sales OS — المرحلة 3 — وحدة 3 (3.3): توحيد المتابعات ✅ (منطق مختبَر)

وسّعت المهمة الموجودة (`PlanItem`) بدل بناء نظام مهام تاني:
- **حالات حقيقية** (`TaskStatus`): مفتوحة / تمّت / **أُلغيت** / **مؤجلة** — منفصلة عن مجرد تغيير التاريخ. `setTaskStatus` بيختم `completedAt` ويحافظ على `done` القديم متزامن؛ و`rescheduleTask` بيغيّر التاريخ **من غير** ما يلمس الحالة. ✅
- **المصدر** (`TaskSource`): يدوي / زيارة / عرض سعر / فرصة / مساعد — والمهام اللي بينشئها الـAI بقت متوسومة `AI`. (بيمهّد لمنع التكرار في 3.4). ✅
- **ربط بفرصة**: المهمة ممكن تترط بصفقة من فرص نفس العميل، وبتتربط تلقائيًا بالعميل (customerId). ✅
- **توافق قديم**: `statusEnum()` بترجع للـ`done` القديم لو `status` فاضي؛ الحقول كلها إضافية بـdefaults واتضافت لموديل الديسكتوب (round-trip). مفيش ترحيل قسري — المهام القديمة بتفضل زي ما هي (مفتوحة/تمّت). ✅
- **الواجهة**: كارت المهمة بيعرض وسم «أُلغيت/مؤجلة» ومصدرها؛ وشيت المهمة فيه اختيار الحالة + ربط الفرصة.
- **مبدأ «مش نسختين»**: الأساس اتحطّ (المهمة تحمل opportunityId + source)؛ التوحيد الكامل في شاشة اليوم (المهام + الزيارات + الخطوات القادمة للفرص من غير عدّ مزدوج) هييجي في 3.5.

**ما اختُبر**: 5 اختبارات JVM جديدة (تعيين الحالة القديمة، أولوية الحالة الصريحة، الملغى/المؤجل مش «تمّت»، وسوم المصدر، round-trip). **إجمالي المشروع: 48 اختبار JVM + 15 اختبار Emulator.** بناء الأندرويد وكومبايل الديسكتوب ناجحان.
**متبقٍ في المرحلة 3**: 3.4 الأتمتة المحددة (عرض مُرسل→متابعة+تحديث مرحلة، زيارة بإجراء→مهمة، بمنع التكرار)، 3.5 شاشة اليوم الموحّدة.

## Sales OS — المرحلة 3 — وحدة 4 (3.5): شاشة اليوم الموحّدة ✅ (منطق مختبَر، أوفلاين)

- **أجندة موحّدة** في شاشة اليوم بتجمع من كل المصادر بقواعد محلية (بدون AI، تشتغل أوفلاين): المهام + متابعات الزيارات + الخطوات القادمة للفرص + انتهاء عروض الأسعار. `buildAgenda` نقية ومختبَرة. ✅
- **٣ أقسام بأسباب واضحة**: **متأخر** / **المطلوب اليوم** / **يحتاج قرار** — وكل عنصر بيوضّح سبب ظهوره (موعد / متأخر / اليوم / فرصة بلا خطوة / عرض منتهي). ✅
- **بدون عدّ مزدوج**: الفرصة اللي متابعتها أصلاً مهمة مفتوحة مبتظهرش مرتين (المهمة بتمثّلها). ✅
- **الفرص المغلقة/المؤجلة مبتطاردش** المستخدم يوميًا؛ الفرصة النشطة بلا خطوة قادمة بتظهر كـ«يحتاج قرار»؛ العرض المُرسل المنتهي بيظهر لقرار. ✅
- **أزرار سريعة**: إتمام المهمة، وتأجيلها يوم (reschedule مش تغيير حالة)، وفتح الزيارة. ✅
- كل الحسابات محلية (مقارنة تواريخ) — **مفيش AI ولا إنترنت** لحساب المتأخر/المواعيد، زي ما الخطة بتصر.

**ما اختُبر**: 8 اختبارات JVM جديدة (توزيع المهام/الزيارات على الأقسام، استبعاد المكتمل/الملغى، سبب «موعد» بالوقت، فرصة بلا خطوة = اقتراح، الفرص المغلقة مبتطاردش، منع العدّ المزدوج، عرض مُرسل منتهي). **إجمالي المشروع: 56 اختبار JVM + 15 اختبار Emulator.** بناء الأندرويد وكومبايل الديسكتوب ناجحان.
**متبقٍ في المرحلة 3**: 3.4 الأتمتة المحددة (قواعد كود: عرض مُرسل→متابعة+اقتراح مرحلة، زيارة بإجراء→مهمة، بمنع تكرار عبر source+refId).

## Sales OS — المرحلة 3 — وحدة 5 (3.4): الأتمتة المحددة (قواعد كود) ✅ (منطق مختبَر) — المرحلة 3 اكتملت

قواعد **deterministic في الكود** (مفيش AI)، مع مفتاح إيقاف في الإعدادات («الأتمتة الذكية»، مفعّل افتراضيًا):
- **عرض سعر → SENT**: (١) إنشاء متابعة تلقائية **مرة واحدة** (dedup عبر `source=QUOTE` + `sourceRef=quote.id` — إعادة الحفظ/المحاولة مبتكررش)، ميعادها = تاريخ انتهاء العرض لو موجود، وإلا **اقتراح +3 أيام قابل للتعديل** (مش حقيقة مفروضة). (٢) تقديم الفرصة المرتبطة لمرحلة «عرض سعر» **بس** لو كانت نشطة وأقدم (مش رجوع للخلف ولا صفقة مغلقة) — مسجّل في سجل المراحل. + ختم تاريخ الإرسال. ✅
- **إغلاق صفقة (WON/LOST)**: إلغاء تلقائي لمتابعاتها **المفتوحة القادمة** بس (المكتملة/الماضية/صفقة تانية ما تتلمسش). ✅
- **إتمام متابعة لفرصة نشطة بلا خطوة**: مغطّى أصلاً في أجندة اليوم (بتظهر تحت «يحتاج قرار») — مفيش اختراع وعد. ✅
- **بدون Automation Builder عام** — القواعد دي بس، وكلها قابلة للإيقاف. ✅

**ما اختُبر**: 6 اختبارات JVM جديدة (dedup لكل عرض، المسودة ما تشغّلش، تاريخ الانتهاء ثم الاقتراح، التقديم من مرحلة أقدم نشطة بس، إلغاء المفتوح-القادم المرتبط فقط). **إجمالي المشروع: 62 اختبار JVM + 15 اختبار Emulator.** بناء الأندرويد وكومبايل الديسكتوب ناجحان.

**✅ المرحلة 3 اكتملت بالكامل**: 3.1 الأنشطة + السجل الزمني · 3.2 عروض الأسعار · 3.3 توحيد المتابعات · 3.5 شاشة اليوم الموحّدة · 3.4 الأتمتة المحددة.
**المتبقّي من الخطة**: المرحلة 4 (AI Copilot — استخراج/صياغة/أوامر)، المرحلة 5 (التحليلات والمراجعة الأسبوعية)، المرحلة 6 (المعرفة/الفريق/المرفقات/التكاملات/ويندوز).

## Sales OS — المرحلة 4 — وحدة 1 (4.1): تجهيز ما قبل الزيارة ✅ (السياق مختبَر)

- **زر «جهّزني للزيارة»** في صفحة العميل → ملخص ذكي (Gemini بمفتاح المستخدم): ملخص الوضع، هدف مقترح للزيارة، أسئلة مهمة ناقصة، وعود/التزامات سابقة، وأمور تحتاج تحقّق. ✅
- **من المسجّل فقط**: `PrevisitContext` (نقية) بتبني السياق من بيانات العميل الحقيقية بس، وبتعلّم الناقص بوضوح **«(not recorded)»** عشان النموذج مايخترعش، والسياق **محدود** (آخر ٦ تفاعلات) — مايتبعتش كل قاعدة البيانات. ✅
- **فرص متعددة**: لو فيه أكتر من فرصة نشطة، بيطلب **تختار الفرصة** (أو ملخص عام) قبل ما يجهّز — مايخلطش صفقتين. ✅
- **الخصوصية والتحكم (4.6)**: أول استخدام بيوضّح **إيه اللي هيتبعت** ولمين (Gemini) ويطلب موافقة (مخزّنة)؛ مفيش مفاتيح بتتبعت؛ زر **إلغاء** أثناء الطلب وحالة **فشل + إعادة محاولة**؛ الميزة محتاجة مفتاح، وباقي التطبيق شغّال من غيره. ✅
- **تنويه**: كل مخرجات مبنية على المسجّل بس ولازم تتراجع.

**ما اختُبر**: 4 اختبارات JVM لباني السياق (إدراج العميل ووسم الناقص، تفاصيل الفرصة المركّزة، حدّ التفاعلات، إدراج المهام والعروض). *(نداء Gemini نفسه بيتجرّب يدويًا بمفتاح — غير مؤتمت.)* **إجمالي المشروع: 66 اختبار JVM + 15 اختبار Emulator.** بناء الأندرويد وكومبايل الديسكتوب ناجحان.
**متبقٍ في المرحلة 4**: 4.3 صياغة المتابعة/الإيميل، 4.2 الاستخراج المنظّم بعد الزيارة (مقارنة الحالي/المقترح)، 4.4 أوامر داخل التطبيق، 4.5 اسأل بياناتك + Coach.

## Sales OS — المرحلة 4 — وحدة 2 (4.3): صياغة المتابعة/الإيميل ✅ (السياق مختبَر)

- **زر «اكتب رسالة (AI)»** في صفحة العميل → يصيغ: بعد الزيارة / متابعة عرض / طلب اجتماع / معلومات منتج / سؤال المشتريات. اختيار **النبرة** (ودّي/رسمي/مختصر) و**اللغة** (EN/AR) و**المستلم** (من جهات الاتصال) و**الفرصة**. ✅
- **مخرجات قابلة للتعديل**: عنوان + نص؛ وأزرار **نسخ / إيميل (فتح تطبيق البريد بالمستلم والعنوان والنص) / مشاركة** — **بدون إرسال مباشر** (الإرسال من تطبيق الجهاز، التكامل في المرحلة 6). ✅
- **حواجز أمان في الـprompt**: ممنوع وعد بخصم/توفّر/تسليم مش موجود في السياق، ممنوع كلمة «مرفق» (مفيش مرفق)، والسعر/المنتج المجهول يتسأل عنه أو placeholder — **مايختلقش قيمة**. ✅
- **من المسجّل فقط**: `DraftContext` (نقية) بتبني السياق من بيانات حقيقية وتعلّم الناقص. نفس بوابة الموافقة/المفتاح/الإلغاء بتاعة 4.1. ✅

**ما اختُبر**: 4 اختبارات JVM لباني سياق المسودة. **إجمالي المشروع: 70 اختبار JVM + 15 اختبار Emulator.** بناء الأندرويد وكومبايل الديسكتوب ناجحان.
**متبقٍ في المرحلة 4**: 4.2 الاستخراج المنظّم بعد الزيارة، 4.4 أوامر داخل التطبيق، 4.5 اسأل بياناتك + Coach.

## Sales OS — المرحلة 4 — وحدة 3 (4.2): الاستخراج المنظّم بعد الزيارة ✅

- **زر «استخرج التفاصيل (AI)»** في محرّر الزيارة (بيظهر لما فيه ملاحظة) → يستخرج حقائق منظّمة: الاحتياج، المشكلة، المنتج، حالة الميزانية، توقيت الشراء، المنافس، الاعتراض، الخطوة القادمة — **يملأ الحقل فقط لو مذكور صراحة** (temperature 0.1، مفيش استنتاج). ✅
- **مقارنة الحالي/المقترح قبل التطبيق**: شيت مراجعة بيعرض لكل حقل القيمة **الحالية** في الفرصة (مشطوبة) مقابل **المقترحة**، مع **تشيك بوكس لكل حقل** (اقتراحات محتاجة تأكيد). ✅
- **بدون إنشاء بالتخمين**: لو مفيش فرصة للعميل، التطبيق بيعمل فرصة جديدة **بإجراء صريح منك** (زر تطبيق) — مش تلقائيًا بتشابه الاسم. ✅
- **عملية واحدة**: كل الحقول المختارة بتتطبّق في `upsert` واحد — مفيش نص تغييرات لو اتلغى. الملاحظات الأصلية AR/EN بتفضل زي ما هي. ✅
- نفس بوابة الموافقة/المفتاح/الإلغاء.

**ما اختُبر**: البناء + الاختبارات القائمة (70 JVM + 15 Emulator) ناجحة؛ الاستخراج نفسه (نداء Gemini) بيتجرّب يدويًا بمفتاح.
**متبقٍ في المرحلة 4**: 4.4 أوامر داخل التطبيق، 4.5 اسأل بياناتك + Coach.

## Sales OS — المرحلة 4 — وحدة 4 (4.4): أوامر داخل التطبيق ✅ (منطق مختبَر)

- **مساعد أوامر** (زر في شاشة المهام) → تكتب أمر بلغتك، الـAI **يفسّره لنيّة منظّمة** (CommandIntent) بس، و**التطبيق** هو اللي يتحقّق وينفّذ عبر عمليات Store — مفيش وصول حر لقاعدة البيانات. ✅
- **الأوامر المدعومة**: إضافة متابعة، نقل متابعة لتاريخ تاني، بحث. مثال: «ضيف متابعة لأحمد الخميس ١٠ص» → بيحلّ «أحمد» و«الخميس→تاريخ فعلي» و«١٠ص» ويعرض **بطاقة تأكيد** قبل التنفيذ. ✅
- **تحقّق قبل التنفيذ**: حلّ العميل (مطابقة دقيقة أو غير ملتبسة، وإلا تنبيه)، تاريخ فعلي yyyy-MM-dd من الـAI، وتأكيد صريح؛ **منع التكرار** (`taskExists`) قبل الإضافة؛ النقل بيحصل عبر `rescheduleTask`. ✅
- **حدود صريحة**: مفيش حذف ولا إرسال خارجي ولا تغيير صلاحيات تلقائي؛ الكتابة كلها بعد تأكيدك؛ التاريخ النسبي بيتحلّ مرة واحدة (مش «بعد ٣ أيام» مفروضة). نفس بوابة الموافقة/المفتاح. ✅

**ما اختُبر**: 3 اختبارات JVM (منع التكرار مع تجاهل الملغى/التاريخ المختلف وعدم حساسية الحالة، والافتراضي UNKNOWN). **إجمالي المشروع: 73 اختبار JVM + 15 اختبار Emulator.** بناء الأندرويد وكومبايل الديسكتوب ناجحان.
**متبقٍ في المرحلة 4**: 4.5 اسأل بياناتك + Sales Coach.

## Sales OS — المرحلة 4 — وحدة 5 (4.5): اسأل بياناتك + Sales Coach ✅ — المرحلة 4 اكتملت

- **اسأل بياناتك** (Tools → اسأل بياناتك): استعلامات محددة على السجلات، **الأرقام محسوبة محليًا بالكود** (offline، دقيقة): متابعات متأخرة، عروض مُرسلة بلا متابعة مسجّلة، فرص نشطة بلا صاحب قرار، فرص بلا خطوة قادمة، عملاء محتملون — كل استعلام بيعرض العدد + قائمة السجلات (drill-down). ✅
- **Sales Coach**: يجمّع الإشارات المحسوبة في نقاط «محتاج انتباه» (العدد = الأساس)، وزر اختياري **«لخّص بالـAI»** — الـAI **يلخّص المحسوب فقط**، ممنوع يخترع/يعيد حساب أرقام أو يجزم بسبب خسارة/احتمال فوز. ✅
- **صدق**: «بلا متابعة مسجّلة» ≠ «العميل ما ردّش»؛ والفرص المغلقة مستبعدة؛ وملاحظة إن كله محلي (المتأخر حتى اليوم). ✅
- **بدون vector DB ولا Agent framework** — استعلامات محددة على السجلات الحالية زي ما الخطة طلبت. ✅

**ما اختُبر**: 5 اختبارات JVM لـ`DataQueries` (عروض بلا متابعة، فرص بلا صاحب قرار/خطوة، محتملون بفلتر مدينة، متأخر يستبعد المكتمل/المستقبل). **إجمالي المشروع: 78 اختبار JVM + 15 اختبار Emulator.** بناء الأندرويد وكومبايل الديسكتوب ناجحان.

**✅ المرحلة 4 اكتملت**: 4.1 تجهيز الزيارة · 4.3 صياغة المتابعة/الإيميل · 4.2 الاستخراج المنظّم · 4.4 أوامر داخل التطبيق · 4.5 اسأل بياناتك + Coach. (نداءات Gemini تُختبر يدويًا بالمفتاح؛ الأجزاء المنطقية مغطّاة بالاختبارات.)
**المتبقّي من الخطة**: المرحلة 5 (التحليلات والمراجعة الأسبوعية) والمرحلة 6 (المعرفة/الفريق/المرفقات/التكاملات/ويندوز).

## Sales OS — المرحلة 5 — وحدة 1 (5.1 + 5.2): قاموس المقاييس + لوحة التحليلات ✅ (محسوب مختبَر)

- **محرّك تحليلات نقي** (`Analytics`) بتعريفات صريحة، محسوب بالكود offline: عملاء محتملون جدد (بتاريخ الإنشاء + العلاقة)، تفاعلات مكتملة (زيارات + أنشطة بنتيجة) **مع فصل «بلا رد»**، عروض مُرسلة (بتاريخ الإرسال)، مكسوب/خاسر (بتاريخ الإغلاق stageChangedAt)، **Pipeline لقطة حالية**، قيمة المكسوب، **مبيعات فعلية (طلبات) منفصلة عن الـPipeline**، والالتزام بالمتابعة (منجز في وقته / المستحق). ✅
- **كله per-currency** — العملات مفصولة ومش بتتجمع أبدًا؛ والتواريخ المجهولة بتُستبعد مش بتتحسب صفر. ✅
- **لوحة التحليلات** (Tools → التحليلات): فلتر فترة (أسبوع/شهر/آخر ٣٠ يوم) + **مقارنة بفترة سابقة بنفس الطول بأرقام فعلية** (▲/▼ + العدد)، وأقسام Pipeline/المكسوب/المبيعات لكل عملة، وملاحظة إن الكل محلي. ✅
- **فصل النشاط عن الإيراد**: نشاط الزيارات/التفاعلات منفصل عن المكسوب والمبيعات الفعلية (زي ما الخطة طلبت). ✅

**ما اختُبر**: 8 اختبارات JVM لـ`Analytics` (المحتملون بالتاريخ/العلاقة، فصل بلا رد، مكسوب/خاسر بتاريخ الإغلاق per-currency، Pipeline النشط، المبيعات من الطلبات، العروض بتاريخ الإرسال، الالتزام، وعدم القسمة على صفر). **إجمالي المشروع: 86 اختبار JVM + 15 اختبار Emulator.** بناء الأندرويد وكومبايل الديسكتوب ناجحان.
**متبقٍ في المرحلة 5**: 5.3 المراجعة الأسبوعية (+ تلخيص AI اختياري)، 5.4 مراجعة الصفقات الخاسرة، 5.5 تطوير المستخدم.

## Sales OS — المرحلة 5 — وحدة 2 (5.3 + 5.4 + 5.5) ✅ — المرحلة 5 اكتملت

- **5.3 المراجعة الأسبوعية** (Tools → المراجعة الأسبوعية): تقرير محسوب محليًا يجمّع Analytics + DataQueries — «اللي تحقق» (مكسوب/خاسر/تفاعلات/عروض/محتملون مع فرق فعلي عن الأسبوع اللي فات)، «يحتاج انتباه» (متأخر/عروض بلا متابعة/فرص بلا صاحب قرار أو خطوة)، **تركيز واحد مقترح** (قاعدة `WeeklyReview.focus`) مع زر «أضفه كمهمة»، وملخص **AI اختياري** (يلخّص الحقائق المحسوبة بس). بيحفظ الفترة + وقت الإنشاء؛ بداية الأسبوع حسب إعداد التطبيق. الأرقام حقائق والملخص اقتراح. ✅
- **5.4 مراجعة الصفقات الخاسرة** (Tools → الصفقات الخاسرة): لكل خسارة آخر مرحلة (من سجل الانتقالات)، مدة الصفقة، سبب الخسارة + مصدره (صرّح/استنتاج)، المنافس؛ **أنماط متكررة فقط لما تتكرر (٢+)** — مش من حالة واحدة؛ أسئلة مراجعة ثابتة؛ **مفيش تغيير للسبب المسجّل ولا أحكام AI**. ✅
- **5.5 تطوير المستخدم**: في المراجعة الأسبوعية — هدف سلوكي أسبوعي واحد قابل للقياس بيتحفظ ويتراجع لما الأسبوع يتغيّر؛ **مفيش Personal Score /100**، ومفيش استنتاج مهارة من كثرة فتح التطبيق. ✅

**ما اختُبر**: 7 اختبارات JVM جديدة (`WeeklyReview.focus` بالأولوية والحالات، `LostDeals` آخر مرحلة/الأنماط بعتبة ٢). **إجمالي المشروع: 93 اختبار JVM + 15 اختبار Emulator.** بناء الأندرويد وكومبايل الديسكتوب ناجحان.

**✅ المرحلة 5 اكتملت**: 5.1 قاموس المقاييس · 5.2 لوحة التحليلات · 5.3 المراجعة الأسبوعية · 5.4 الصفقات الخاسرة · 5.5 تطوير المستخدم.
**النسخة الأساسية من Sales OS (مراحل 1–5) جاهزة لتقييم أسبوع عمل حقيقي.** المتبقّي: المرحلة 6 (المعرفة/الفريق المشترك/المرفقات/التكاملات/استكمال ويندوز) — وحدات توسّع اختيارية حسب الاستخدام الفعلي.

## Sales OS — المرحلة 6 — وحدة 1 (6.1): المنتجات والمعرفة + مكتبة الاعتراضات + مطابقة المنتج ✅ (منطق مختبَر)

- **معرفة المنتجات** (Tools → المنتجات والمعرفة): سجل معرفة **منفصل عن المخزون/السعر** — المصنّع، الموديل، المواصفات، الاستخدامات، الخامات، القيود، «مناسب لـ»، المصدر، وتاريخ التحديث، مع **علامة «موثّق / يحتاج تحقّق»** (امتلاك brochure لا يثبت التوفّر). CRUD كامل. ✅
- **مطابقة المنتج بالاحتياج** (`ProductMatch` نقية): تبحث بكلمات الاحتياج في الاستخدامات/المواصفات/الخامات/الاسم، وتُرتّب حسب التطابق، وتوضّح **«طابق في إيه»** وتعرض حالة التوثيق — بدون وعد بأداء/توفّر غير مثبت. ✅
- **مكتبة الاعتراضات** (Tools → مكتبة الاعتراضات): الاعتراض + سؤال استكشاف + دليل/حالة استخدام + رد مقترح قابل للتعديل. CRUD كامل. ✅
- **توافق ومزامنة**: `ProductKnowledge`/`Objection` أُضيفت لكل طبقات البيانات (backup + دمج ثلاثي + عزل الحساب + round-trip) على الأندرويد والديسكتوب. ✅

**ما اختُبر**: 5 اختبارات JVM (`ProductMatch`: المطابقة بالكلمات، الترتيب بالتطابق، حمل علامة التوثيق و«طابق في»، تجاهل الاحتياج الفارغ/القصير، round-trip + الدمج). **إجمالي المشروع: 98 اختبار JVM + 15 اختبار Emulator.** بناء الأندرويد وكومبايل الديسكتوب ناجحان.

**متبقٍ من المرحلة 6 (وحدات توسّع تحتاج بنية تحتية/أجهزة — مش قابلة للإنجاز/الاختبار من هنا وحدي):**
- 6.2 بيانات الفريق المشتركة (نطاق شركة/شخصي بصلاحيات backend) — الأساس اتعمل في 1.5 (عضوية/إسناد/قواعد)؛ المشاركة الكاملة للعملاء/الزيارات تحتاج نقل لمجموعات company-scoped + اختبار متعدد الأجهزة.
- 6.3 مزامنة المرفقات — تحتاج Firebase Storage (تفعيل + قواعد) واختبار رفع/تنزيل.
- 6.4 تكامل البريد/التقويم — تحتاج OAuth لمزوّد (Gmail/Outlook) وتسجيل تطبيق.
- 6.5 استكمال واجهات ويندوز (فرص/عروض/مهام/مساعد/مراجعة) — شغل واجهة كبير محتاج تشغيل فعلي للنسخة المكتبية للاختبار.
- 6.6 المشروعات الكبرى وخريطة الحساب — عند بدء الاستخدام الفعلي متعدد الأطراف.

## Sales OS — المرحلة 6 — وحدة 2 (6.5): استكمال واجهات ويندوز — جزئي ✅ (اتكتب، محتاج اختبارك)

- **النسخة المكتبية**: أُضيف تبويبان جديدان في النسخة المكتبية بيقرأوا نفس البيانات المتزامنة:
  - **الفرص (Opportunities)**: قائمة مجمّعة حسب المرحلة (بنفس ترتيب/مسميات مراحل الأندرويد + تعيين FOLLOW القديمة → تأهيل)، بتعرض العنوان/العميل/القيمة/الخطوة القادمة. (قراءة) ✅
  - **المهام (Tasks)**: قائمة المهام مع **تعليم «تمّ» من الكمبيوتر** (بيتزامن مع الموبايل عبر `toggleTask`). ✅
- **البيانات**: `opportunities` و`tasks` بقت state عامة في الـStore المكتبي (كانت pass-through مخفية). ✅
- **كومبايل ناجح** + بُني app-image يشتغل على: `desktop\build\compose\binaries\main\app\VisitFlow\VisitFlow.exe` — **محتاج تشغّله وتجرّبه** (مقدرش أشغّل واجهة رسومية من هنا).

**متبقٍ في 6.5** (شغل واجهة أكبر + محتاج تشغيل فعلي):
- محرّر/عرض العروض (Quotes) على الديسكتوب، المساعد الذكي، والمراجعة الأسبوعية — تحتاج نقل طبقة الحساب/الـAI للموديول المكتبي.
- توحيد enum أنواع الزيارة القديم في واجهة الديسكتوب.
- رقم إصدار موحّد + تحديث تلقائي للنسخة المكتبية.

## Sales OS — المرحلة 6 — وحدة 2 (6.5 تكملة): عروض + مراجعة أسبوعية على الديسكتوب ✅ (اتكتب، محتاج اختبارك)

- **نقل طبقة الحساب للنسخة المكتبية**: `SalesLogic.kt` (enums + helpers + QuoteMath) + نسخ `Analytics.kt` و`DataQueries.kt` (WeeklyReview/LostDeals) — **نفس التعريفات بالظبط** عشان الأرقام تطابق الموبايل. ✅
- **تبويب العروض (Quotes)**: قائمة مجمّعة حسب الحالة مع الإجمالي المحسوب (QuoteMath). (قراءة) ✅
- **تبويب المراجعة الأسبوعية**: «اللي تحقق» بفرق فعلي (▲▼) عن الأسبوع اللي فات (نافذة ٧ أيام)، «يحتاج انتباه»، و«تركيز مقترح» — كله محسوب محليًا بنفس منطق الموبايل. ✅
- `quotes`/`activities`/`orders` بقت مكشوفة (قراءة) في الـStore المكتبي. ✅
- **كومبايل ناجح** + اتبنى app-image محدّث على: `desktop\build\compose\binaries\main\app\VisitFlow\VisitFlow.exe`.

**متبقٍ في 6.5** (أكبر/يحتاج تشغيل فعلي): المساعد الذكي على الديسكتوب (يحتاج نقل AiFormatter + أوامر)، توحيد enum أنواع الزيارة القديم في واجهة الديسكتوب، ورقم إصدار + تحديث تلقائي للنسخة المكتبية.

## Sales OS — المرحلة 6 — وحدة 2 (6.5 اكتمل تقريبًا): مساعد الديسكتوب + توحيد الأنواع + الإصدار ✅ (محتاج اختبارك)

- **توحيد أنواع الزيارة على الديسكتوب**: استبدلت الـenum القديم (FIRST/FOLLOW/DELIVER/COLLECT/COMPLAINT/OTHER) بالـ**12 نوع** بتاعة الموبايل + `mapLegacyVisitType` — فالزيارة اللي اتعملت على الموبايل بنوع جديد بتظهر صح على الكمبيوتر (والقديمة بتتحوّل صح). محرّر الزيارة المكتبي بقى يعرض الـ12. ✅
- **مساعد ذكي على الديسكتوب**: شريط أوامر في تبويب المهام — الـAI يفسّر (`parseCommand`) والتطبيق يتحقّق وينفّذ عبر Store: إضافة متابعة (مع منع تكرار)، نقل متابعة، بحث — ببطاقة تأكيد قبل التنفيذ. أُضيفت `addTask`/`rescheduleTask` للـStore المكتبي. ✅
- **الإصدار**: النسخة المكتبية بقت `1.8.0`. ✅
- **كومبايل ناجح** + app-image محدّث على: `desktop\build\compose\binaries\main\app\VisitFlow\VisitFlow.exe`.

**المتبقّي الوحيد في 6.5**: **تحديث تلقائي** للنسخة المكتبية — محتاج قرار استضافة (مكان يُرفع عليه ملف التحديث + manifest) عشان النسخة تفحص وتنزّل التحديث؛ ده إجراء بنية تحتية من عندك.

**✅ المرحلة 6 المُنجَز منها**: 6.1 المعرفة/الاعتراضات/المطابقة · 6.5 استكمال ويندوز (ماعدا التحديث التلقائي).
**متبقٍ (يحتاج بنية تحتية/أجهزة)**: 6.2 مشاركة الفريق الكاملة · 6.3 مزامنة المرفقات (Firebase Storage) · 6.4 تكامل بريد/تقويم (OAuth) · 6.6 المشروعات الكبرى · التحديث التلقائي للديسكتوب.

## Sales OS — المرحلة 6 — وحدة 3 (6.2): مشاركة بيانات الفريق — النواة المفروضة في الـbackend ✅ (منشور ومختبَر)

- **سجلات مشتركة على مستوى الشركة**: مجموعات `companies/{cid}/customers` و`companies/{cid}/opportunities` بمصفوفة صلاحيات مفروضة في الـbackend:
  - **قراءة**: أي عضو (نطاق الشركة).
  - **إنشاء**: أي عضو، وبس كـ«مالكها» (`ownerUid == self`).
  - **تعديل**: المدير (أي سجل)، أو المندوب على سجله هو بس — و**المندوب ميقدرش ينقل الملكية** (`ownerUid` ثابت لغير المدير).
  - **حذف**: المدير أو مالك السجل.
  - إزالة العضو **مابتمسّش** سجلاته → شغله/تاريخه محفوظ.
- **مختبَر بالـEmulator**: +9 اختبارات جديدة (قراءة العضو، رفض غير العضو، المندوب يعدّل سجله مش سجل غيره، منع نقل الملكية، المدير يعدّل أي سجل، الإنشاء كمالك بس) → **إجمالي 24 اختبار Emulator، كلها ناجحة**. القواعد **اتنشرت** فعلاً على المشروع.
- **العميل**: `CompanyRepository` بقى فيه `teamOpps` (listener) + `shareOpportunity` + `deleteTeamOpportunity`؛ وشاشة الفريق فيها **«خط أنابيب الفريق»**: قائمة الفرص المشتركة (العنوان/العميل/القيمة/المالك) + زر مشاركة + حذف (للمالك/المدير — والـbackend بيفرضها كمان). ✅

**ما اختُبر**: القواعد بالـEmulator (24/24). بناء الأندرويد ناجح. **إجمالي المشروع: 98 اختبار JVM + 24 اختبار Emulator.**
**متبقٍ في 6.2** (يحتاج جهازين/حسابين للاختبار الفعلي): نقل العملاء/الزيارات الشخصية للنطاق المشترك بالكامل، ولوحة مدير موسّعة على السجلات المشتركة. الأساس المفروض في الـbackend جاهز ومنشور.

## Sales OS — المرحلة 6 — وحدة 4 (6.4): تكامل التقويم — الشريحة بدون OAuth ✅

- **إضافة للتقويم من غير OAuth**: زر «أضف للتقويم» في شيت المهمة → بيفتح تطبيق التقويم على الجهاز (أي تقويم: Google/Outlook) مملوء بالعنوان/الوصف/التاريخ/الوقت/المدة عبر `CalendarContract` intent — يوم كامل لو مفيش وقت. يشتغل أوفلاين وبدون تسجيل. ✅
- **البريد**: التسليم لتطبيق البريد موجود من 4.3 (بدون إرسال مباشر).

**متبقٍ في 6.4 (يحتاج تسجيل OAuth منك)**: التكامل الحقيقي ثنائي الاتجاه (ربط رسائل/مواعيد بالسجل، تحديث تلقائي، حفظ external ID) محتاج **تسجيل تطبيق OAuth** عند Google Cloud أو Microsoft + client credentials + شاشة موافقة — ده إجراء بنية تحتية من عندك. WhatsApp يفضل مشاركة يدوية.

## Sales OS — المرحلة 6 — وحدة 5 (6.3): المرفقات — الكود جاهز ✅ (الرفع الفعلي محتاج تفعيلك)

- **الكود اتكتب بالكامل** (commit `8952d51`): `firebase-storage` dependency + موديل `Attachment` (id/نوع السجل/اسم/mime/حجم/مسار محلي/مسار سحابي/مرفوع/تاريخ) + مزامنة كاملة (backup + دمج ثلاثي + عزل الحساب) على **الأندرويد والديسكتوب** round-trip. ✅
- **StorageRepo**: يستورد ملف مختار لتخزين محلي أولاً، ثم **رفع مؤجّل** لـ`users/{uid}/attachments/{id}`، وتنزيل عند الفتح. بيشتغل **local-only** لو Storage مش مفعّل — فالميتاداتا بتتزامن والملف ما بيضيعش. ✅
- **الواجهة**: كارت مرفقات في صفحة العميل (إضافة/عرض/حذف). ✅
- **متبقٍ (إجراء منك)**: تفعيل **Firebase Storage** (خطة Blaze) + نشر قواعد `storage.rules` — لحد ما تعمل ده، الرفع/التنزيل بين الأجهزة معطّل (بيفضل محلي). الاختبار الفعلي محتاج جهازين. ⬜

## Sales OS — المرحلة 6 — وحدة 6 (6.6): المشاريع الكبيرة + خريطة الحساب ✅ (مبني، الاختبار على الجهاز)

- **مشروع كبير** (`Project`): بيجمّع كذا فرصة تحت مبادرة واحدة للعميل، وبيربط **أصحاب القرار** (من جهات اتصال العميل بأدوارهم) — القيمة المجمّعة **لكل عملة على حدة** (مفيش جمع بين عملات). CRUD كامل + شاشة «المشاريع وخريطة الحساب» في الأدوات.
- **خريطة الحساب**: داخل المشروع بتعرض جهات اتصال العميل مصنّفة بأدوار القرار (صاحب قرار/مؤثّر/فني/مشتريات/بوابة)، وكل واحد تقدر تعلّمه كصاحب مصلحة في المشروع.
- **مزامنة كاملة** (backup + دمج ثلاثي + عزل الحساب + ترحيل) على الأندرويد والديسكتوب round-trip. كومبايل الاتنين ناجح.

## Sales OS — المرحلة 6 — وحدة 2 (6.5 تكملة): تحديث تلقائي للنسخة المكتبية عبر GitHub ✅ (مبني)

- **`DesktopUpdater`**: بيقرأ `desktop-update.json` من الريبو (raw githubusercontent، مجاني)، يقارن رقم البناء، وعند وجود أحدث بيفتح صفحة التنزيل (GitHub Release بيستضيف المثبّت الكبير مجاناً). زر «تحقق من التحديثات» في إعدادات الديسكتوب > عن التطبيق. **6.5 اكتملت.**

## Sales OS — المرحلة 6 — وحدة 5 (6.3): مزامنة المرفقات عبر Google Drive ✅ (مبني + إعداد Cloud تم)

- **مجاني عبر Google Drive** (نطاق `drive.file` غير الحسّاس — التطبيق يشوف ملفاته بس، بدون تحقّق من جوجل): زر «اربط Google Drive» في الإعدادات؛ المرفقات بتترفع لـDrive بتاع المستخدم والـfileId بيتخزّن على المرفق فيتنزّل على أي جهاز تاني. من غير Firebase Storage / Blaze.
- **إعداد Google Cloud تم** (مشروع visitflow-3f533): تفعيل Drive API + شاشة موافقة (In production/External) + **Android OAuth client** (الحزمة `com.visit.flow` + SHA-1 مفتاح التوقيع). ممكن تاخد من 5 دقايق لبضع ساعات لحد ما تسري.
- **متبقٍ (اختبارك)**: تسجيل الدخول بحساب جوجل على الجهاز واختبار الرفع/التنزيل بين جهازين فعليين.

## Sales OS — تحسين UX: كارت تفاصيل المهمة في خط سير اليوم ✅ (مبني)

- الضغط على أي محطة في **خط سير اليوم** بيفتح **كارت تفاصيل كامل** (`PlanDetailSheet`): العميل، التاريخ، الوقت، المدة، المصدر، الفرصة المرتبطة، النتيجة، وتاريخ الإتمام — + أزرار سريعة (اتجاهات/اتصال/أضف للتقويم)، تبديل تمّت/مفتوحة، نقل (بكرة/بعد بكرة/اختيار يوم)، تعديل inline (عبر `updatePlan`) وحذف. الدايرة على الرِّيل فضلت للتعليم تمّت، والضغط المطوّل لسه بيدخل التحديد المتعدد. ✅
- **مبني** في APK (commit `8952d51`) — كومبايل ناجح.
