# VisitFlow — إعادة المراجعة في 16 سبتمبر 2026

## الحكم

ميزات جديدة أُضيفت، لكن مشاكل المراجعة السابقة لم تُصلح في المسارات التي فُحصت، وظهرت ثغرة مشابهة في صلاحيات العملاء والفرص المشتركة. لا يُنصح باعتبار النسخة جاهزة للاعتماد على المزامنة أو عزل الفريق قبل الإصلاح.

آخر commit ظهر عند نهاية المراجعة: `151195a` (المساعد الصوتي). تغيرت حالة Git أثناء العمل من تغييرات محلية إلى commit جديد؛ النتائج أدناه تخص الملفات المقروءة والاختبارات المنفذة في هذه المراجعة، ولا تعني مراجعة شاملة لكل تغيير لاحق.

## ما تم التحقق منه فعليًا

- نجح بناء Release لأندرويد، ونجح compileKotlin لويندوز.
- اختبارات JVM: **99 اختبارًا، 98 ناجحًا وفشل واحد**. الفاشل هو اختبار المراجعة السابق لتكرر تعارض المزامنة.
- اختبارات Firestore الموجودة: **24 ناجحًا** على المحاكي المحلي.
- أضفت اختبارات منع كتابة العضو المُزال: **3 اختبارات فاشلة**؛ الطلبات التي يجب رفضها نجحت.
- لم تُستخدم بيانات Firebase الفعلية ولم تُنشر قواعد أو حزم. أُوقف المحاكي بعد الاختبار.
- لم أختبر على جهاز فعلي أو جهازين، ولم أختبر تفويض Drive/Calendar الفعلي أو المساعد الصوتي على الهاتف.

## النتائج مرتبة بالأولوية

### [P1] 1. إزالة العضوية لا تمنع التعديل والحذف — مثبت بالمحاكي

المواقع: `firestore.rules:78` و`:96` و`:103`.

فرع المندوب في الإسناد وفرع مالك العميل/الفرصة لا يشترطان العضوية الحالية. مستخدم أُزيل من الشركة يستطيع، بطلب مباشر وهو ما زال مسجل الدخول، تغيير done لإسناده، وتعديل عميل يملكه، وحذف فرصة يملكها.

النتيجة الفعلية للاختبارات الثلاثة: `Expected request to fail, but it succeeded.`

الإصلاح المطلوب: اشتراط المصادقة والعضوية الحالية في جميع فروع تعديل وحذف السجلات المشتركة، مع إبقاء قيود الملكية والحقول. شغّل اختبارات الإزالة قبل نشر القواعد المصححة. هذا تحقق من القواعد المحلية؛ القواعد المنشورة لم تُفحص.

إعادة التشغيل: شغّل محاكي Firestore محليًا على `127.0.0.1:18080` ثم:

```powershell
node tmp/review-rules-2026-09-16.mjs
```

### [P1] 2. تعارض ثانٍ لنفس السجل يفقد تعديلًا — ما زال يفشل

الموقع: `app/src/main/java/com/sales/visits/SyncMerge.kt:82`.

عند وجود `v1~c` قديم، تعديل v1 إلى B على جهاز وC على الآخر لا يحفظ النسخة الخاسرة الجديدة. الناتج في الاختبار B وZ فقط، مع conflicts=0، فتضيع C دون تنبيه.

المطلوب: هوية ثابتة وفريدة لكل نسخة متعارضة، تمنع الفقد وتمنع تضاعف نفس النسخة عند إعادة الدمج. راجع النسخة المكتبية أيضًا.

```powershell
.\gradlew.bat :app:testDebugUnitTest --tests com.sales.visits.SyncMergeReviewTest --init-script tmp/review-tests.init.gradle --offline
```

### [P1] 3. تبديل الحساب يمحو البيانات غير المرفوعة — لم يتغير

الموقع: `app/src/main/java/com/sales/visits/Store.kt:453`.

prepareForAccount ما زالت تفرغ القوائم وتحفظ الفراغ عند اختلاف الحساب، بدون حفظ حالة مستقلة للحساب السابق. التعديلات غير المرفوعة تضيع عند التبديل. المطلوب حفظ بيانات محلية مستقلة لكل حساب واستعادتها عند العودة، مع اختبار أوفلاين.

### [P1] 4. نجاح رفع قديم قد يستبدل تعديلًا أحدث — لم يتغير

الموقع: `app/src/main/java/com/sales/visits/CloudAccount.kt:167`، ومسار push المقابل في ويندوز.

بعد انتهاء المعاملة تُستعاد merged إذا اختلفت عن snapshot التي بدأ بها الرفع، دون مصالحة تعديل محلي حدث أثناء الشبكة. كما لا يتحقق callback من بقاء نفس جلسة الحساب. المطلوب التحقق من الجلسة ودمج التعديلات الأحدث قبل تطبيق النتيجة، واختبار ترتيب وصولها مع تعديل محلي وتبديل حساب.

### [P1] 5. فشل قراءة إصدار أحدث لا يوقف الكتابة — لم يتغير

الموقع: `app/src/main/java/com/sales/visits/CloudAccount.kt:160` مع `Store.kt:429`.

رفض threeWayMerge لإصدار غير مدعوم يعيد null، والرفع يستبدله بـsnapshot المحلية ثم يكتبها. هذا قد يمحو حقول الإصدار الأحدث. المطلوب إيقاف المعاملة وطلب التحديث عند عدم التوافق؛ لا استخدام نسخة محلية كبديل عند فشل الدمج. طبّق على ويندوز أيضًا.

### [P2] 6. التقويم ليس ثنائي الاتجاه وتكرار الإضافة يولّد أحداثًا مكررة

المواقع: `CalendarSync.kt` و`Store.kt:650`.

- توجد عمليات POST/PUT/DELETE من التطبيق إلى Google فقط. لا يوجد سحب أحداث أو مصالحة لتعديل أُجري داخل Google Calendar، رغم وصف الميزة كثنائية الاتجاه في الخطة والتعليق.
- addTaskToCalendar تستدعي create كل مرة دون فحص calendarEventId الموجود أو وجود طلب إنشاء جارٍ. الضغط مرة ثانية قد ينشئ حدثًا آخر ويستبدل ID المخزن، ويبقى القديم بلا رابط.
- ترجع true بمجرد بدء Thread، وتُهمل أخطاء التحديث والحذف؛ لا توجد قائمة عمليات معلقة لإعادة المحاولة بعد انقطاع الشبكة.

المطلوب: تحديث الحدث الموجود بدل إنشائه ثانية، ومنع الطلب المتكرر، وإظهار نتيجة العملية الفعلية وحفظ عمليات إعادة المحاولة. لتنفيذ الوصف المطلوب أضف مسار Google → التطبيق مع سياسة تعارض، أو صحح حالة الخطة إلى اتجاه واحد حتى التنفيذ.

### [P2] 7. تاريخ نهاية حدث اليوم الكامل غير صحيح

الموقع: `app/src/main/java/com/sales/visits/CalendarSync.kt:49`.

start.date وend.date يأخذان اليوم نفسه. Google يعرّف النهاية بأنها حصرية؛ لحدث ليوم واحد يجب أن تكون النهاية اليوم التالي. الحمولة الحالية تصف مدة صفر وتحتاج تصحيحًا واختبارًا، بما يشمل نهاية الشهر والسنة. [مرجع Google Calendar الرسمي](https://developers.google.com/workspace/calendar/api/v3/reference/events).

### [P2] 8. تنزيل مرفق متوقف قد يُفتح لاحقًا كملف مكتمل

المواقع: `DriveSync.kt:98` و`StorageRepo.kt:72`.

التنزيل يكتب مباشرة في الملف النهائي. إذا توقف بعد كتابة جزء، يبقى الملف موجودًا. وعلى جهاز يحتفظ بنفس localPath القادم من المزامنة، محاولة الفتح التالية قد تكتفي بوجوده وتتجاوز التنزيل، وتفتح ملفًا ناقصًا.

المطلوب: تنزيل إلى ملف مؤقت ثم نقله إلى المسار النهائي بعد النجاح، وعدم اعتبار الوجود وحده دليل اكتمال، وتنظيف الملف الجزئي عند الفشل. أضف اختبار تنزيل يتوقف في المنتصف ثم يعاد.

## تقدم فعلي مقارنة بالمراجعة السابقة

- إصدار أندرويد وupdate.json متطابقان محليًا عند **1.9.3 / build 18**؛ ملاحظة بقاء الرقم 9 لم تعد قائمة. لم يتحقق تنزيل تحديث منشور فعليًا.
- أُضيفت مشاركة العملاء والفرص، ومرفقات Drive، ومشروعات وخريطة حساب، وعروض PDF وهامش ربح، ومساعد صوتي.
- updater ويندوز يفحص ملف التحديث ويفتح صفحة تنزيل؛ ليس تنزيلًا وتثبيتًا تلقائيين مع التحقق من الحزمة.
- المشاركة الحالية تنقل بيانات مختصرة. shareOpportunity ينشئ سجلًا جديدًا بمرحلة NEW، وليست مزامنة مستمرة للفرصة الأصلية مع الزيارات والعروض والتاريخ. لا يُعد ذلك اكتمال نطاق الفريق الكامل في الخطة.
- تنفيذ المشروع الكبير مرتبط بعميل واحد وفرصه؛ نطاق الشركات المتعددة والمقاولين وتسجيل الموردين يحتاج مقارنة منفصلة قبل إعلان اكتمال الخطة الأصلية.

## ما يسلم إلى المنفذ

ابدأ بالنتائج 1–5؛ لا تضف مزيدًا من الميزات قبل إصلاحها. شغّل الاختبارات الأصلية والإضافية معًا. بعد ذلك أصلح التقويم وتنزيل المرفقات، ثم اختبر جهازين فعليين. حدّث UX-ROADMAP بناءً على الأدلة، وافصل «الكود موجود» عن «اختُبر» وعن «نُشر».

هذه المراجعة لم تعدّل كود الإنتاج. الملفات الجديدة تخص تقرير المراجعة واختبار الصلاحيات فقط.
